# Golden references (pending)

M4/M5 requires permitted, privacy-safe golden data: an aligned 112x112 crop, the expected
input tensor, the raw `embedding` output and the reference embedding after L2 normalization.
None is present yet. Embeddings are biometric data: do not commit real vectors, faces or
samples that are not explicitly cleared for use.
- **Status:** planned — no golden fixtures present

