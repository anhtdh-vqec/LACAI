# features

Feature IDs, dependencies, permissions, compatibility and resource requirements.

`feature_catalog.example.json` is synthetic input for the versioned neutral feature
catalog contract. It demonstrates model-role and bounded processor requirements; it is
not an active feature grant, product configuration or qualification result. A trusted
activation layer must still select source/feature assignments and enforce entitlement.
